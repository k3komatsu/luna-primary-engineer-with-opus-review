# Managed Ponytail reference

`SKILL.md` is populated here by `scripts/install.sh` from the private upstream Ponytail checkout. It is intentionally not bundled as a second globally discoverable skill.
